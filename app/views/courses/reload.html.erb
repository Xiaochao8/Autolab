<h2>Error Parsing your Course Configuration File:</h2>

<h4>Error</h4>
<div>
  <pre><%= @error.to_s %></pre>
</div>

<h4>Options</h4>
<ul class="collection">
  <li class="collection-item">Directly edit the configuration file (via the file system) and <%= link_to Sgtn.t('courses.reload', 'views'), reload_course_path(@course, @assessment), data: {method: "post"} %></li>
  <li class="collection-item">Upload a new configuration file <%= link_to Sgtn.t('courses.here', 'views'), edit_course_path(@course) %></li>
</ul>

<h4>Stack Trace</h4>
<div>
  <pre><%= @error.backtrace.join("\n") %></pre>
</div>
